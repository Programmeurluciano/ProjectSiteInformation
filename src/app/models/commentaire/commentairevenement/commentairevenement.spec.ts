import { Commentairevenement } from './commentairevenement';

describe('Commentairevenement', () => {
  it('should create an instance', () => {
    expect(new Commentairevenement()).toBeTruthy();
  });
});
